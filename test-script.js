const { generateObject } = require('ai'); const { google } = require('@ai-sdk/google'); const { z } = require('zod'); require('dotenv').config(); async function test() { try { const result = await generateObject({ model: google('gemini-2.5-flash'), schema: z.object({ weeks: z.array(z.object({ title: z.string(), duration: z.string(), topics: z.array(z.string()) })) }), prompt: 'You are an AI Coach. The user wants to become AI Engineer. They miss: Cloud, Math. Generate a 4 week roadmap. Keep topics very brief (2-4 words).' }); console.log('Success!', JSON.stringify(result.object, null, 2)); } catch (err) { console.error('Error:', err.message); } } test();
